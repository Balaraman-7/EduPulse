{
  "name": "edupulse-root",
  "version": "1.0.0",
  "description": "EduPulse - Full Stack Early Warning System",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n \"BACKEND,FRONTEND\" -c \"blue.bold,cyan.bold\" \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "concurrently --kill-others-on-fail -n \"BACKEND,FRONTEND\" -c \"blue.bold,cyan.bold\" \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "seed": "npm run seed --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
